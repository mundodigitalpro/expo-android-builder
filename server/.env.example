# Server configuration
PORT=3001

# Authentication token for API requests
# Generate a secure random token and use the same value in the mobile app Settings
AUTH_TOKEN=your-secure-token-here

# Base directory where Expo projects will be created
# For Termux on Android, use: /data/data/com.termux/files/home/app-builder-projects
# For VPS/Linux, use: /app-builder-projects (or your preferred path)
PROJECTS_BASE_PATH=/data/data/com.termux/files/home/app-builder-projects

# Environment mode
NODE_ENV=development

# ============================================
# Expo/EAS Configuration
# ============================================
# Your Expo account username (from expo.dev)
EXPO_OWNER=your-expo-username

# Package prefix for Android/iOS apps (reverse domain notation)
# Example: com.yourcompany or com.yourname
EXPO_PACKAGE_PREFIX=com.example

# ============================================
# GitHub Actions Integration (Optional)
# ============================================
# Generate a Personal Access Token at: https://github.com/settings/tokens
# Required scopes: repo, workflow
GITHUB_TOKEN=your_github_personal_access_token_here
GITHUB_REPO_OWNER=your-github-username
GITHUB_REPO_NAME=expo-android-builder

# ============================================
# Claude Code CLI Configuration (Optional)
# ============================================
# If using Claude Code in Docker, mount credentials from host:
# volumes:
#   - /path/to/.claude:/home/node/.claude:rw
# CLAUDE_CONFIG_DIR=/home/node/.claude

